services:
  app:
    build: .
    ports:
      - "8800:8800"
    volumes:
      - app-data:/app/data
    environment:
      - JWT_SECRET=change-me-to-a-secure-random-string
      - DB_PATH=/app/data/app.db
    restart: unless-stopped

volumes:
  app-data:
